<%= form_for(@student) do |f| %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>

  <div class="field">
    <%= f.collection_select :house_id, House.all, :id, :name %><br>
  </div>

<!--   <div class="field">
    <%= #f.collection_check_boxes :spell_ids, Spell.all, :id, :name %><br>
  </div>
 -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
