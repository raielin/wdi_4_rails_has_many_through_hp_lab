<p id="notice"><%= notice %></p>

<h2><%= @student.name %>'s Profile</h2>

<p>
  <strong>Age:</strong>
  <%= @student.age %>
</p>

<p>
  <strong>House:</strong>
  <%= @student.house.name %>
</p>

<p>
  <strong>Curriculum Status</strong>
</p>

<p>
   <table>
    <thead>
      <tr>
        <th>Course</th>
        <th>Spells Learned</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @spells.each do |course| %>
        <tr>
          <td><%= course.course_name%></td>
          <td><%= course.spell_id.name%></td>
          <td><%= link_to 'View More', spell %></td>
          <td><%= link_to 'Edit', edit_spell_path(spell) %></td>
          <td><%= link_to 'Delete', spell, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</p>

<%= link_to 'Edit Student Profile', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %>
